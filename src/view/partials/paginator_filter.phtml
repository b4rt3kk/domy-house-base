<?php
/* @var $form \Base\Form\AbstractForm */
$id = 'paginator_filter_' . str_replace('\\', '_', get_class($form));
/* @var $paginator \Base\Paginator */
$filterData = $paginator->getFilterData();
?>
<?php if ($form instanceof Base\Form\AbstractForm) : ?>
<div class="accordion">
  <div class="accordion-item">
    <h2 class="accordion-header">
      <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#<?php echo $id; ?>" aria-expanded="true" aria-controls="<?php echo $id; ?>">
        Filtr wyszukiwania <?php echo !empty($filterData) ? '&nbsp;<strong>[Tak]</strong>' : ''; ?>
      </button>
    </h2>
    <div id="<?php echo $id; ?>" class="accordion-collapse collapse">
      <div class="accordion-body">
        <?php echo $this->partial('base/form', ['form' => $form]); ?>
      </div>
    </div>
  </div>
</div>
<?php endif; ?>